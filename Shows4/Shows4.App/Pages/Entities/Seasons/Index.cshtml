@page
@model Shows4.App.Pages.Entities.Seasons.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Season[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Season[0].Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Season[0].RatingGlobal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Season[0].Episode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Season) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RatingGlobal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Episode.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
